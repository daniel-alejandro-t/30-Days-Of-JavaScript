/*
Slope is m = (y2-y1)/(x2-x1). Find the slope between point (2, 2)
and point(6,10)
*/

let x = parseInt(prompt('Insert x: '))
let y = parseInt(prompt('Insert y: '))

alert('m = ' + (y * 2 - y * 2) / ( x * 2 - x *1))