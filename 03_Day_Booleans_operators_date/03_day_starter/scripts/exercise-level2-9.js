// Writ a script that prompt a user to enter hours and rate per hour.
// Calculate pay of the person?

let hours = parseFloat(prompt('Insert hours: '))
let ratePerHour = parseFloat(prompt('Insert rate per hour: '))

alert('Pay: ' + ( hours * ratePerHour))